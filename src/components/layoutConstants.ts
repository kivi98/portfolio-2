/**
 * Single source of truth for the fixed-header clearance every page needs.
 * Keep loading/error/not-found states on the same values so the layout
 * doesn't jump between states.
 */
export const PAGE_TOP_PADDING = { xs: "96px", md: "140px" };
